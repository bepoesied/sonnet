image: nixos/unstable
packages:
  - nixos.devenv
  - nixos.cachix
  - nixos.secretspec
sources:
  - git@git.sr.ht:~drangon/sonnet
tasks:
  - setup-cachix: |
      cachix use devenv
  - devenv-test: |
      cd sonnet
      devenv test
